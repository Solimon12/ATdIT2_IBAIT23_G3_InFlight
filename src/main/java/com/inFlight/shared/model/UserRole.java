package com.inFlight.shared.model;

/**
 * UserRole is an enumeration that represents the different roles a user can have in the application.
 * It includes roles such as PASSENGER, PILOT, ATTENDANT, PHOTOGRAPHER, and ATC.
 */
public enum UserRole {
    PASSENGER,
    PILOT,
    ATTENDANT,
    PHOTOGRAPHER,
    ATC
    // Add more roles as needed
}
